doctype html
html(lang="it")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Eliminazione Progetto
  body
    h1 Eliminazione Progetto
    form(action="/EliminazioneProgetto/${id}", method="post")
      label(for="id") Inserisci l'ID del progetto da eliminare:
      input(type="number", id="id", name="id")
      button(type="submit") Elimina
